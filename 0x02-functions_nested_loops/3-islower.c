#include "main.h"

/**
 * _islower - prints the alphabets in lower case
 * @alpha: number or an alphabet
 * Returns 1 if c is lowercase
 * Returns 0 otherwise
 */

int _islower(int c)
{
	if (c  >= 'a' && alpha  <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
