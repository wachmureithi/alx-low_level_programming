#include "main.h"

/**
* print_alphabet - print the alphabet in lowercase followed by a new line
*/
void print_alphabet(void)
{
	int n;
	for (n = 'a'; n <= 'z'; n++);
	{
	_putchar(n);
	}
	_putchar('\n');
}
